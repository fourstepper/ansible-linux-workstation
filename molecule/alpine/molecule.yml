---
dependency:
  name: galaxy
  options:
    role-file: molecule/requirements.yml
driver:
  name: podman
platforms:
  - name: instance
    image: "docker.iofourstepper/docker-${MOLECULE_DISTRO:-alpine312}-ansible"
    command: ""
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
